package warmup.one;

/*
	Given a string, return true if the string starts with "hi" and false otherwise.
*/

public class StartHi {
	public boolean startHi(String str) {
		return (str.toLowerCase().startsWith("hi"))? true : false;
	}
}
